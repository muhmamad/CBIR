To run the program you must have the following libraries installed:numpy and PyQT5.

To run the program open the UI file and run it in your IDE. Click "load image" and click on an image from the dataset. Next click "search image"
to find all relevant similar images.
